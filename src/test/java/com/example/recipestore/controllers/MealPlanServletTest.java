package com.example.recipestore.controllers;

import org.junit.jupiter.api.Test;

class MealPlanServletTest {

    // Mockito, mocks, stubbing

    @Test
    void generateMealPlan() {
        // dummyRequest = request.addParam()

       //var mockDao =  Mockito.mock(RecipeDAO);
        // mock.when(thing).thenReturn(expectedValue)
        //new RecipeServlet(mockDao);

        //  response =servlet.doPost(dummy test data)

        //assert(response.getField("name")) //AssertJ
        //        .equals(expectedName)
    }
}